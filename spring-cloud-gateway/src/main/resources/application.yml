spring:
  application:
    name: spring-cloud-gateway-sample
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/

server:
  port: 9090
